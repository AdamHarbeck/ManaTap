import React from "react";

// This is where the user will create a new discussion topic. It will save to storage and add to forum page.
const NewForum = () => {
    return(
        <div styles={styles.container}>

        </div>
    )
}
export default NewForum;

const styles = {
    container:{

    }
}