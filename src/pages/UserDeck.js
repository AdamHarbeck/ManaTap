import React from "react";

// This will be created when the user clicks on their deck. The info from local will be sent here.
const UserDeck = ({texts}) => {
    let text = {texts};
    console.log(text)
    return(
        <div style={styles.container}>

        </div>
    )
}
export default UserDeck;

const styles = {
    container:{

    }
}